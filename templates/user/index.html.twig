{% extends 'base.html.twig' %}

{% block content %}
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-body">
          <div class="row">
            <div class="col-md-4">
              {# {% if user.photo %}
              <img src="{{ asset('uploads/photos/' ~ user.photo) }}" alt="Photo de profil" class="img-fluid rounded-circle">
              {% else %}
              <img src="{{ asset('images/default-user.png') }}" alt="Photo de profil" class="img-fluid rounded-circle">
              {% endif %} #}
            </div>
            <div class="col-md-8">
              <h1>{{ user.nom }} {{ user.prenom }}</h1>
              <p>Téléphone : {{ user.telephone }}</p>
              <p>Email : {{ user.email }}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}

