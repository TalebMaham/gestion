<!DOCTYPE html>
<html>
<head>
    <title>Projet Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Center the card */
        .center-card {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* You can adjust this as needed */
        }
        
        /* Add a scroll bar to the card body */
        .card-body-scroll {
            max-height: 300px; /* Adjust the maximum height as needed */
            overflow: auto;
        }

        /* Position the button at the bottom-right */
        .bottom-right-button {
            position: absolute;
            bottom: 10px;
            right: 10px;
        }
    </style>
</head>
<body>
    <div class="center-card">
        <div class="card" style="width: 40rem; position: relative;">
            <div class="card-body card-body-scroll">
                <h5 class="card-title">{{ projet.titre }}</h5>
                <p class="card-text">{{ projet.description }}</p>
                <p class="card-text"><strong>Budget Max:</strong> ${{ projet.budgetMax }}</p>
                <p class="card-text"><strong>Budget Min:</strong> ${{ projet.budgetMin }}</p>
                <p class="card-text"><strong>Pourcentage de Rentabilité:</strong> {{ projet.pourcentageRentabilite }}%</p>
                <p class="card-text"><strong>Villes:</strong> {{ projet.villes|join(', ') }}</p>
            </div>
            <a href= {{path("add_project", {'id' : projet.id})}} class="btn btn-success bottom-right-button">Lancer la recherche</a>
        </div>
    </div>
</body>
</html>
