<!DOCTYPE html>
<html>
<head>
    <title>Opportunity Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Center the card */
        .center-card {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* You can adjust this as needed */
        }
        
        /* Style the red percentage value */
        .red-percentage {
            color: red;
        }

        /* Style the green button */
        .green-button {
            position: absolute;
            bottom: 20px;
            right: 20px;
        }
    </style>
</head>
<body>
    <div class="center-card">
        <div class="card" style="width: 40rem; height : 25rem; ">
            <div class="card-body">
                <h5 class="card-title">{{ opportunity.title }}</h5>
                <p class="card-text">{{ opportunity.description }}</p>
                <p class="card-text"><strong>City:</strong> {{ opportunity.city }}</p>
                <p class="card-text"><strong>Budget:</strong> ${{ opportunity.budget }}</p>
                <p class="card-text"><strong>Monthly Profit:</strong> ${{ opportunity.monthlyProfit }}</p>
                <p class="card-text"><strong>Annual Profit:</strong> ${{ opportunity.annualProfit }}</p>
                <p class="card-text"><strong>Profit Percentage:</strong>
                    <span class="red-percentage">{{ opportunity.profitPercentage }}%</span>
                </p>
                <!-- Green button -->
                <a href = {{path('associate_opportunity_show', {'opportunityId' : opportunity.id})}} class="btn btn-success green-button">On y va ! </a>
            </div>
        </div>
    </div>
</body>
</html>
